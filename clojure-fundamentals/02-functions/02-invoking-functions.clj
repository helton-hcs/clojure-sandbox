(ns clojure-fundamentals.invoking-functions)

; Invoking functions

( (fn [message] (println message) ) ; operation = anonymous function
  "Hello, world"                  ; arguments
)

;; => "Hello, world"